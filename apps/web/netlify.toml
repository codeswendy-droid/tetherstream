[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_API_BASE_URL = "https://tetherstream-production-e99c.up.railway.app/api/v1"
  VITE_TELEGRAM_BOT_USERNAME = "titanstream_bot"

# SPA Fallback Redirects for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
